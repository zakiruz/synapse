{
	"name": "bonds",
	"version": "0.1.2",
	"description": "Obsidian plugin: links as first-class notes",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"lint": "eslint src/",
		"build": "tsc -noEmit -skipLibCheck && eslint src/ && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"esbuild": "^0.21.0",
		"eslint": "^9.0.0",
		"obsidian": "latest",
		"tslib": "^2.6.0",
		"typescript": "^5.4.0",
		"typescript-eslint": "^8.0.0"
	}
}
